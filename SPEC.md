# ani-rate 仕様メモ

個人用のアニメ採点ツール。スマホ・PC どちらのブラウザからも読み書きできる。

- 公開URL（予定）: `https://papk-x.github.io/ani-rate/`
- リポジトリ: `https://github.com/papk-x/ani-rate`
- 版: 機能追加ごとに 0.01 上げる（`index.html` のヘッダ表記が正）

---

## 1. 採点の考え方

平均方式（1話ごと10点満点 → 12話平均）は採用しない。理由は2つ。

- 平均を取るとばらつきが 1/√n に縮み、作品間の差が消える（12話なら約1/3.5）
- 終盤で跳ねる構成・伏線回収・余韻が拾えず、「毎話そこそこ」の作品が有利になる

採用する方式：

| フェーズ | やること | 位置づけ |
|---|---|---|
| 各話 | ☆ / ◎ / ○ / △ / × / -（未回答）の6段階 ＋ 一言メモ | 記録用。採点はしない |
| 完走時 | 各話マークとメモを見返して 1点刻みで100点満点 | **これが正式スコア** |
| 参考値 | ☆=10 / ◎=8 / ○=6 / △=4 / ×=2 で換算した平均 | 表示のみ。最終点には使わない |
| 30日後 | 余韻が残っているかで再評価 | 任意 |

`-` は「見たが評価を保留」の意味。参考平均の分母から外す。

点数入力は 10の位と1の位を上下ボタンで動かす方式（1.03〜）。
以前の5点刻み21段階のボタンは廃止した。刻みを粗くしても迷う時は迷うため、
入力の速さより「85と84を分けたい時に分けられる」ことを優先している。

初期値は `—`（未入力）。上下ボタンを押すまで点数を付けたことにしない。

### 点数なしで残す（1.03〜）

採点モーダルの「点数なしで保存」で、点数を付けずに視聴記録だけ残せる。`noScore: true` が立つ。

- 平均・年別平均・点数分布・再評価候補 … すべて対象外（`typeof score === 'number'` で絞っているため）
- 採点待ちのバッジ … 数えない。自分で「付けない」と決めたものなので催促しない
- 一覧 … 「記録のみ」と表示。フィルタ「記録のみ（点数なし）」で絞れる
- 状態（視聴中／完走／中断）は変えない。視聴中のまま記録だけ残せる

未採点（まだ決めていない）と 記録のみ（付けないと決めた）は別物として扱う。
前者だけがバッジに出る。

### 点数の色（1.03〜）

50点以上は10点ごとに色を変える。50点未満は 30〜49 と 〜29 の2色に分ける。

| 帯 | 色 |
|---|---|
| 100 | 紫 `--sc100` |
| 90〜99 | 青 `--sc90` |
| 80〜89 | 緑 `--sc80` |
| 70〜79 | 黄緑 `--sc70` |
| 60〜69 | 黄 `--sc60` |
| 50〜59 | 橙 `--sc50` |
| 30〜49 | 赤 `--sc30` |
| 〜29 | 濃い赤 `--sc0` |

一覧の点数バッジと、統計の年別平均で同じ色を使う。ダークテーマ用に明度を上げた値を別に持つ。

### 基準作品（アンカー）

100 / 90 / 80 / 70 / 60 / 50 の各ラインに実在の作品を紐付けて設定タブに保存する。
これがないと年をまたいだ時に採点がインフレ・デフレする。

---

## 2. データ構造

保存先は localStorage（端末ごと）と 非公開 Gist（端末間共有）。

```json
{
  "version": 1,
  "updatedAt": "ISO8601",
  "anchors": { "100": "", "90": "", "80": "", "70": "", "60": "", "50": "" },
  "works": [
    {
      "id": "w...",
      "seq": 775,
      "title": "作品名",
      "year": 2026,
      "yearEst": false,
      "airDate": "2026-01-09",
      "airYear": 2026,
      "airCour": 1,
      "media": "tv | movie | ova | sp",
      "status": "watching | done | dropped",
      "episodes": [ { "no": 1, "mark": "◎", "note": "", "at": "ISO8601" } ],
      "epScores": [ { "no": 1, "score": 8.0 } ],
      "score": 85,
      "noScore": false,
      "scoredAt": "ISO8601",
      "reScore": null,
      "reScoredAt": null,
      "note": "",
      "source": "app | import"
    }
  ]
}
```

- `seq` … 視聴順。**大きいほど新しい**。取り込み時は xlsx の A列をそのまま使う
- `episodes` … アプリで付けた ☆◎○△×- の記録
- `epScores` … xlsx 由来の話ごと点数（10点満点系）。表示のみで採点には使わない
- `year` … 視聴年。`yearEst: true` は放送クールと視聴順から推定した年で、確定値ではない
- `airDate` / `airYear` / `airCour` … 放送開始日と放送クール（1=1月期 … 4=10月期）。
  `airDate` は編集画面から手で入れる。`airYear`・`airCour` は xlsx のE列由来
- 編集画面で視聴年を書き換えると `yearEst` は false（確定）になる。触らなければ推定のまま残る

---

## 3. 画面

| タブ | 内容 |
|---|---|
| 視聴中 | 視聴中の作品。6段階を1タップで次話に記録。**「次は N 話」を大きく出す**。1話戻す／直前にメモ／振り返り／完走 |
| 採点 | 完走済みで未採点のもの。未採点件数をバッジ表示 |
| 一覧 | 全作品。タイトル検索・並べ替え（視聴順／点数順）・状態フィルタ。**行をタップで振り返り** |
| 統計 | 自己平均、直近20/50件平均、年別平均、点数分布、再評価候補 |
| 設定 | 文字サイズ、アンカー、Gist同期、インポート／エクスポート、データ消去 |

スマホでの入力を最優先にしている。マークは見た直後に数タップで済むこと。

開いていたタブは `anirate_tab` に覚え、次回起動時はそこから始める（1.03〜）。
文字サイズと同じく**端末ごとの設定**で、Gist には送らない。PC は一覧、スマホは視聴中、
のように使い分けられる。

保存後にタブが飛ばないようにもした（1.03〜）。一覧から編集・採点しても一覧に戻る。
視聴中に移るのは「作品を新規追加した時」だけ。

### コメントの振り返り（1.03〜）

書いたメモを読み返せないと、完走時の採点に使えない。そのため以下を用意した。

| 場所 | できること |
|---|---|
| 一覧の行をタップ | 振り返りモーダル。全話のマークとコメントを一覧で読む |
| 話のマス（チップ）をタップ | その話のマークの付け直しとメモの書き直し・削除 |
| 採点モーダル | メモの付いた話だけを本文表示。点数を決めながら読み返せる |

一覧のマスは、メモが付いている話の右上に点を出す。どこに書いたかが一目で分かる。

### モーダルのボタン配置（1.03〜）

どの画面でも同じ位置に置く。押し間違いを減らすため。

| 位置 | ボタン |
|---|---|
| 右下 | 保存・確定（`.btn.pri`） |
| 左下 | 閉じる、削除 |

フッタ（`.foot`）はモーダル内でスクロールしても下に貼り付く。
話数が多い作品でも、下までスクロールせずに保存できる。

**枠の外（背景の暗い部分）を押すと閉じる**（1.03〜）。`e.target` が `.modal` 自身の時だけ
反応させる。中身を押した時は `.inner` 側が target になるので閉じない。
振り返りの上に話ごとメモを重ねている時は、上に乗っている方だけが閉じる。

### 書きかけの保護（1.03〜）

`openModal()` で開いた時点の入力内容を `dirtyBase` に控え、閉じる時に見比べる。
違っていれば「書きかけの内容があります。保存せずに閉じますか。」と確認する。
枠の外を押した時も、閉じるボタンを押した時も同じ扱い（どちらも `tryClose()`）。

見ているのは入力欄だけでなく、**まだ保存していない選択**も含む。

| モーダル | 判定に使う値 |
|---|---|
| 作品の編集 | タイトル・視聴年・放送開始日・状態・メモ |
| 採点 | 一言メモ、選択中の点数（`scPick`） |
| 話ごとメモ | メモ、選択中のマーク（`epPick`） |
| 振り返り | 入力欄が無いので判定しない。常にそのまま閉じる |

保存後の `closeModal()` は確認しない。保存済みなので書きかけではない。

### 一覧の行の高さ（1.03〜）

1画面に入る件数を優先し、`.list-item` の余白と行間だけを詰めた。**文字サイズは変えていない**
（文字サイズは設定タブの4段階が正で、`html` の `font-size` で一括変更する）。

- 上下の余白 `.53rem` → `.24rem`、行間 1.65 → 1.3、`.sub` の行間 1.25
- 1行あたり約 3.9rem → 約 2.7rem。同じ画面で1.4倍ほど表示できる
- 件数の上限は無し（1.02 までは先頭600件で打ち切っていた）

点数バッジ（`.sc`）は 1.45rem。行の高さはタイトル＋サブ行で決まるため、
数字を大きくしても行は高くならない。幅は3桁（100）が入る 2.9rem で固定し、
行ごとにタイトルの開始位置がずれないようにしている。

---

## 4. Gist 同期 とオフライン動作（1.01〜）

- 非公開 Gist にファイル名 `ani-rate.json` で JSON を1つ置く
- トークンと Gist ID は **localStorage のみ**に保存する
- 変更から4秒後に自動送信（デバウンス）。起動時は Gist の方が新しければ取り込む
- 別端末が先に更新していた場合は、上書き前に確認ダイアログを出す

### 送受信のタイミング（1.03〜）

| 向き | いつ |
|---|---|
| 送信 | 操作の4秒後（デバウンス）／未送信があれば60秒ごと再試行／圏外復帰時／画面復帰時 |
| 受信 | 起動時／**表示中は60秒ごと**／**画面に戻った時**／圏外復帰時／設定タブの手動ボタン |

1.02 までは受信が起動時と手動だけだったため、スマホで入力しても PC のタブを
開きっぱなしだと気づかなかった。1.03 で定期受信を足した。

受信を止める条件は3つ。**未送信を抱えている間**（圏外中の入力を消さないため）、
**送信中**、**モーダルを開いている間**（入力中に画面を差し替えない）。
裏に回ったタブでは見に行かない（`visibilityState`）。

### 通信量（1.03〜）

作品775件で JSON は約250KB ある。1.02 は送信のたびに「取得（250KB）＋送信（250KB）」で
毎回500KB近く流れていた。1.03 では取得に **ETag による条件付きGET** を使う。

- 中身が変わっていなければ `304 Not Modified` が返り、本文は流れない。GitHub のレート制限も消費しない
- 送信（PATCH）のレスポンスの ETag を覚えるので、次の取得はほぼ必ず 304 になる
- 結果、1回の送信は実質「304＋PATCH」＝約250KB。定期受信は変化が無ければほぼ0

`pushing` フラグで送信の二重実行も止めている（250KB を2本同時に流さないため）。

Gist はファイル単位でしか更新できないため、差分送信はできない。
これ以上減らすなら作品を複数ファイルに分割する必要があり、今は見送っている。

### オフライン（電車内での入力を落とさない仕組み）

| 部品 | 役割 |
|---|---|
| `sw.js` | アプリ本体をキャッシュし、圏外でもページを開けるようにする |
| `anirate_pending` | 未送信の変更があるかのフラグ。localStorage に持つ |
| `online` イベント | 圏外から復帰した瞬間に自動で再送 |
| 60秒ごとの再試行 | 復帰イベントを取りこぼした場合の保険 |

ヘッダーの表示で状態が分かる。

- `ローカルのみ` … Gist 未設定
- `同期済み` … 送信済み
- `未送信あり` … オンラインだが送れていない（4秒後か60秒後に再送）
- `圏外` / `圏外・未送信あり` … オフライン

**未送信を抱えている間は、起動時に Gist から取り込まない。**
圏外でためた入力を消さないため。復帰後に送信が済んでから取り込みに戻る。

Service Worker はネット優先・キャッシュ退避の方式。
オンラインなら常に最新が出るので、更新が古いまま固まることはない。
GitHub API へのリクエストはキャッシュしない（同期が壊れるため）。

`file://` では Service Worker が登録されない。オフライン確認は Pages 公開後に行う。

### 版を上げるときの注意

`index.html` のヘッダ表記だけでなく、**`sw.js` の `CACHE` 名も必ず変える**。
変え忘れると、古いキャッシュが配信され続けて更新が反映されない。

### トークンの扱い（厳守）

- **リポジトリに絶対に含めない。** コードへの埋め込み・URLへの埋め込みも禁止
- fine-grained token を推奨。スコープは **gist のみ**
- 端末ごとに1回だけ手入力する

---

## 5. 過去ログ（anime.xlsx）の取り込み

### 元データ

`anime.xlsx` の **`Main1` シートが正本**（他の「コピー」シートは使わない）。

| 列 | 内容 | 取り込み先 |
|---|---|---|
| A | 連番＝視聴順。779が最新、1が最古 | `seq` |
| B | 点数（100点満点） | `score` |
| C | タイトル | `title` |
| D | 状態。`途`=中断 / `未`=未視聴 / `完`=完走 | `status` |
| E | クール・メディア種別（下記） | `airYear` `airCour` `media` `year` |
| F | 話数 | 取り込まない |
| G | 別採点（`alt I R・F4`） | 取り込まない |
| H | 備考（`3D`、`39話～` など） | `note` |
| J列以降 | 話ごと点数。n話 = 列インデックス 8+n | `epScores` |

**E列は「視聴年」ではなく「クール」だった**（1.03で修正）。旧版は `1990〜2100` の範囲で
フィルタしていたため、3桁のクール表記を全部落として3件しか拾えていなかった。

| E列の値 | 意味 |
|---|---|
| `211` `204` `192` … | 放送クール。YY + 期（1=1月期 / 2=4月期 / 3=7月期 / 4=10月期）。`211`＝2021年1月期 |
| `12` `22` | 放送年のみ（2012 / 2022） |
| `2024` `2025` `2026` | **視聴年そのもの**（この3件だけ） |
| `M` `16M` `MO` | 劇場版。数字が付くものは放送年も取れる |
| `O` / `SP` / `r` `s` | OVA / 特番 / 再視聴などの印 |
| `502` | 解釈できないため捨てる（1件） |

J列以降は17話目あたりから `<` やスタジオ名が入っている行がある。数値のセルだけを話ごと点数として拾う。

### 変換

```bash
python tools/xlsx_to_json.py [xlsxのパス] [出力ディレクトリ]
# 既定：F:\…\個人\ani\anime.xlsx → その隣の _import\anime_import.json
```

**出力先はリポジトリの外**（html フォルダには JSON を置かない）。
設定タブの「インポート」でこの JSON を読み込む。

### 重複の判定は連番で行う（1.03〜）

xlsx には**完全に同名の行が3組ある**。分割2クールや続編を同じ名前で記録しているため。

| タイトル | 連番 | 点数 |
|---|---|---|
| Dr.STONE SCIENCE FUTURE（第4期） | 771 / 747 | 75 / 85 |
| みだらな青ちゃんは勉強ができない | 757 / 426 | 80 / 58 |
| 新幹線変形ロボ シンカリオンＺ | 626 / 607 | 82 / 78 |

1.02 まではタイトルで重複判定していたため、この3件が黙って捨てられていた。
1.03 からは **連番（`seq`）が一致する場合だけスキップ**する。連番は xlsx の1行を一意に決めるので、
同名でも連番が違えば別の記録として取り込む。二重取り込みもこれで防げる。

連番を持たない JSON（アプリからの書き出しなど）を読む時だけ、従来どおりタイトルで判定する。

### 取り込み結果（2026-08-14 時点）

- 作品 775 件 / 点数あり 774 件（`ドロヘドロ Season1` のみ点数なし）
- 状態: 完走 603 / 中断 170 / 未視聴 2
- 話ごと点数あり 66 件
- 点数 15〜100、平均 69.01

### 視聴年の扱い

xlsx に視聴日の列は無い。そこで次の2種類を手掛かりに推定する。

| 手掛かり | 強さ | 根拠 |
|---|---|---|
| E列の4桁（2024/2025/2026） | 強 | 視聴年そのもの |
| E列のクールから出した放送年 | 弱 | TVアニメはほぼ放送と同時に見ているため |

連番は視聴順なので、視聴年は連番に対して**単調非減少**になるはず。
そこで候補を連番順に並べ、単調性を満たす最大重みの部分列だけをアンカーに採る。
劇場版・OVA・旧作の後追い視聴（例：連番454の『妖狐×僕SS』＝2012年放送）は
ここで自動的に外れる。アンカー間は連番で線形補間する。

結果：**アンカー 129件 / 年が付いた作品 454件**（旧方式は51件）。

- 2016〜2026年 … 454件（うち推定 451件）
- **年不明 … 321件**（連番 321以下。アンカーより古い側は外挿しない）

外挿しないのは、アンカー区間の実績が年20〜90件と幅があり、
過去の視聴ペースが違いすぎて大きく外れるため。

推定した年は `yearEst: true` が立ち、アプリでは「2021年（推定）」と表示する。
統計タブにも推定件数を出す。前後1年ずれうる前提で読むこと。

- **年別平均** … 2016年以降で有効。ただし大半が推定値
- **直近N件平均** … 全区間で有効。過去の傾向はこちらで見る

今後アプリで登録する分は `scoredAt` が入るので、年別集計が正しく積み上がる。

---

## 6. 公開時の注意

Public リポジトリなので、以下は絶対にコミットしない。`.gitignore` で除外済み。

- `anime.xlsx` および `*.xlsx` … 個人の視聴履歴そのもの
- `_import/` … 変換後の JSON
- GitHub トークン

コミット著者は個人アカウントにすること（会社メールを公開しない）。

```bash
git config user.name "papk-x"
git config user.email "<ID>+papk-x@users.noreply.github.com"
```

---

## 7. 検証状況

`index.html` に対して静的チェックのみ実施済み（1.03 / 2026-08-14）。

- 括弧の対応 … 問題なし
- HTMLから呼ぶ関数（onclick）… すべて定義済み・未使用の定義なし
- JSが参照する id … すべて存在

変換ツールは実データ（anime.xlsx / 775件）で実行して結果を確認済み。

**ブラウザでの実動作は未確認。** 実機での確認が必要。特に以下。

- 6ボタンがスマホ幅で潰れないか
- 一覧 → 振り返り → 話のマスをタップ、の2段モーダルが正しく閉じるか
- 点数の上下ボタンで 0〜100 の端が止まるか
